
    pub static STEP_LEN: f64 = 1.0;
    pub static STEP_ANGLE: f64 = 3.0;
    pub static STEP_TAIL: i32 = 3;
    pub static SCREEN_WIDTH: u32 = 800;
    pub static SCREEN_HEIGHT: u32 = 600;
    pub static BACKGROUND_COLOR: (u8, u8, u8, u8) = (100, 100, 100, 255);

